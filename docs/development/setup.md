<!-- [Doc] | Настройка окружения. Клонирование: git clone, cd svg2web. Toolchain: Rust 1.70+ (rustup), Node 18+ (npm), wasm-pack (cargo install wasm-pack). Сборка всех крейтов: cargo build --workspace, wasm-pack build crates/svg2web-wasm --target web, cd web && npm install && npm run dev. Проверка: cargo test --workspace, cargo clippy -- -D warnings, wasm-pack test --headless. IDE: VSCode extensions (rust-analyzer, Even Better TOML), настройка launch.json для отладки -->
