<!-- [Doc] | Бенчмарки. Criterion: parse_bench (10KB, 100KB, 1MB, 10MB SVG), optimize_bench (сложные пути, много элементов), generate_bench (большие компоненты). Профилирование: cargo flamegraph для CPU (узкие места в парсере), heaptrack для памяти (утечки в WASM), perf для Linux. Цели: parse 10MB < 500ms, analyze < 100ms, optimize < 200ms, generate < 100ms. Регрессия: сравнение с baseline в CI, fail при >10% деградации -->
