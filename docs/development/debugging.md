<!-- [Doc] | Отладка. Логи: tracing subscriber с фильтрами (RUST_LOG=svg2web_core=debug), structured logging для production. Отладка WASM в Chrome DevTools: source maps через wasm-pack --debug, breakpoints в Rust коде, просмотр памяти. Отладка Web Workers: Chrome DevTools → Sources → Threads, сообщения в Console. Типичные проблемы: OOM в WASM (использовать StringPool), CORS для шрифтов (проксирование), stack overflow в глубокой рекурсии (увеличить stack size) -->
