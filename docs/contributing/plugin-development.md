<!-- [Doc] | Разработка плагинов. FormatRenderer trait: fn name(&self) -> &str, fn render(&self, ctx: &RenderContext) -> Result<RenderedOutput>. Реализация: struct MyRenderer, impl FormatRenderer for MyRenderer. Сборка: crate-type = ["cdylib"] в Cargo.toml, cargo build --release. Динамическая загрузка: dlopen, поиск символа, регистрация в FormatRegistry. Примеры: Svelte (генерация .svelte файлов), Solid (генерация .jsx с signals). Тестирование: интеграция с generator, снапшоты выхода -->
