<!-- [Doc] | Стиль кода. cargo fmt: line_width=100, tab_spaces=4, reorder_imports=true, group_imports="StdExternalCrate". clippy: deny unwrap_used, deny expect_used, allow too_many_arguments (до 7), cognitive_complexity_threshold=30. Ошибки: thiserror в core/generator (типизированные ошибки с контекстом), anyhow только в cli (удобство). Документация: rustdoc для всех pub, секция # Examples с runnable кодом, README.md для каждого крейта. Запрещено: unwrap/expect в production коде, println! в библиотеках (использовать tracing), хардкод путей -->
