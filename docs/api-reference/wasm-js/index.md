<!-- [Doc] | Обзор WASM API. Инициализация: import init from 'svg2web-wasm'; await init(). Основные функции: parse_svg(svg: string): object — парсит SVG в промежуточную модель, analyze_svg(svg: string): AnalysisResult — структурный анализ, optimize_svg(svg: string, config?: object): object — оптимизация, extract_assets(svg: string): Asset[] — извлечение ресурсов, serialize_to_json(svg: string): string — сериализация в JSON. Типы данных: примитивы конвертируются автоматически, структуры через serde_wasm_bindgen, бинарные данные через Uint8Array. Ограничения: нет доступа к файловой системе, нет сетевых запросов без CORS, выполняется в single thread (использовать Web Workers) -->
