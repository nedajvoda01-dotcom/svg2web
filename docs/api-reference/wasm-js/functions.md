<!-- [Doc] | Функции WASM. parse(svg: Uint8Array | string): string — парсинг SVG в промежуточный JSON, возвращает JSON-строку с ParseOutput. optimize(svg: Uint8Array | string, config?: string): string — оптимизация SVG, config — JSON-строка OptimizerConfig, возвращает JSON с оптимизированным ParseOutput. extractAssets(json: string, options?: string): string — извлечение ассетов, json — JSON-строка ParseOutput, options — JSON-строка ExtractOptions, возвращает JSON ExtractedAssets. generate(json: string, options: string): string — генерация кода, json — JSON-строка ParseOutput, options — JSON-строка GenerateOptions, возвращает JSON GeneratedCode. convert(svg: Uint8Array | string, options: string): string — полный цикл parse → generate. setPanicHook(): void — установка хука паники для отладки. Сигнатуры, примеры вызова, обработка ошибок -->
