<!-- [Doc] | Типы JS/TS. ParseOutput — интерфейс с полями meta (Meta), structure (SVGElement), geometry (Geometry), styles (Styles), assets (Assets), content (Content). GenerateOptions — интерфейс с полями framework: 'react' | 'vue' | 'vanilla', styling: 'native' | 'tailwind' | 'scoped', responsive: boolean, imageQuality: 'low' | 'medium' | 'high' | 'original', components: { detect: boolean, minSize: number, naming: string }. GeneratedCode — интерфейс с полями html: string, css: string, js: string, components: ComponentCode[], assets: AssetFile[]. AnalysisResult — интерфейс с полями components: Component[], complexity: ComplexityMetrics, hierarchy: HierarchyInfo. Полные TypeScript интерфейсы с JSDoc -->
