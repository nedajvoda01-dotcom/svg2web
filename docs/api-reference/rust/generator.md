<!-- [Doc] | svg2web-generator. Публичные функции: generate(parse_output, options) -> Result<GeneratedCode, Error>, generate_from_file(json_path, options). Типы: GenerateOptions с полями framework (React/Vue/Vanilla), styling (NativeCss/Tailwind/ScopedCss), responsive, image_quality, components (ComponentOptions), output (OutputOptions). GeneratedCode с полями html, css, js, components (Vec<ComponentCode>), assets (Vec<AssetFile>). ComponentCode с полями name, framework, code, path. Framework enum: React, Vue, Vanilla. Styling enum: NativeCss, Tailwind, ScopedCss -->
