<!-- [Doc] | svg2web-cache. Типы: Cache с методами get, set, invalidate, clear. CacheKey с полями hash, version, config_hash, метод from_svg(svg, config). CacheConfig с полями disk_path, memory_size, ttl. DiskCache структура с Sled. MemoryCache структура с LRU. Когда использовать: CLI для инкрементальных сборок, сервер для кэширования повторных запросов. WASM использует только MemoryCache без DiskCache -->
