<!-- [Doc] | svg2web-wasm. Экспортируемые функции: #[wasm_bindgen] parse_svg(svg: &str) -> Result<JsValue, JsValue>, analyze_svg(svg: &str), optimize_svg(svg: &str, config: &JsValue), extract_assets(svg: &str), serialize_to_json(svg: &str) -> Result<String, JsValue>. Типы: StringPool для управления большими строками (allocate, read_chunk, free). WasmCache обертка над MemoryCache. set_panic_hook для отладки. Ограничения: нет файловой системы, нет сетевых запросов (CORS), single-threaded (но в Web Worker) -->
