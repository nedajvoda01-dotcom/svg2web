<!-- [Doc] | assets.json. images: Record<string, { original: { data: base64, mime, width, height }, variants: { '1x' | '2x' | 'default': { file?, inline?, width?, height?, size_kb? } }, used_by: string[] }> — изображения с оптимизированными вариантами. icons: Record<string, { source, type: 'sprite' | 'external' | 'inline', used_by }> — иконки. fonts: Record<string, { files: { woff2?, woff? }, weights, styles }> — шрифты. external: { stylesheets: string[], scripts: string[] } — внешние ресурсы. Баз64 для inline, файлы для копирования, WebP для растровых -->
