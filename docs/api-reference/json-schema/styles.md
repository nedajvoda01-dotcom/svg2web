<!-- [Doc] | styles.json. colors: Record<string, string> — CSS переменные и именованные цвета. gradients: Record<string, { type: 'linear' | 'radial', x1, y1, x2, y2, stops: Array<[color, offset]> }> — градиенты. fonts: Array<{ family, weights, styles, fallback }> — используемые шрифты. typography: Record<string, { family, size, weight, line_height, letter_spacing?, color? }> — типографические стили (h1, h2, body, button, caption). effects: Record<string, { type: 'drop_shadow' | 'blur', ... }> — эффекты. strokes: Record<string, { width, color, dasharray?, linecap, linejoin }> — обводки. CSS-совместимые имена для прямого использования -->
