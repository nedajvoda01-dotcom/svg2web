<!-- [Doc] | content.json. logo: string — текст логотипа. tagline: string — слоган. navigation: { main: Array<{ label, href, active?, icon? }>, secondary: Array<...> } — навигация. contact: { phone, phone_formatted, email, address, work_hours } — контакты. hero: { title, subtitle, description, cta, cta_link, secondary_cta?, image_alt } — hero секция. cards: Array<{ id, title, description, icon, cta, badge?, stats? }> — карточки. footer: { copyright, social: Array<{ platform, url }>, legal: Array<{ label, href }> } — футер. localization: { locale, currency, currency_symbol, date_format } — локализация. Структурированный контент для i18n и CMS интеграции -->
