<!-- [Doc] | geometry.json. bounds: Record<string, { x, y, width, height, rx?, ry? }> — границы каждого элемента. paths: Record<string, { type: 'path', d: string, fill_rule, bounds: [x, y, w, h] }> — SVG path data. transforms: Record<string, { translate?: [x, y], rotate?: number, scale?: [x, y] }> — трансформации. points: Record<string, { x, y }> — именованные точки. Пример: "Vector 123" имеет path d="M1654.83 68.568..." и bounds [1643, 54, 20, 20] -->
