<!-- [Doc] | WASM биндинги. wasm-bindgen: #[wasm_bindgen] для экспорта функций в JS, #[wasm_bindgen(start)] для инициализации. serde_wasm_bindgen: автоматическая конверсия структур в JS объекты. StringPool для больших строк: allocate(String) -> handle (u32), read_chunk(handle, offset, len) -> chunk, free(handle) — предотвращение OOM для SVG > 1MB. Ограничения: нет доступа к файловой системе (no std::fs), нет сетевых запросов без CORS, single-threaded выполнение (решение: Web Workers) -->
