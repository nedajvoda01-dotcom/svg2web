<!-- [Doc] | Сериализация. Модель → JSON через serde. Производительность: zero-copy где возможно, streaming для больших файлов (10MB+), буферизация записи. Формат: единый JSON или 6 отдельных файлов при --split-output. Версионирование: поле version в meta, миграция через transform функции при загрузке старого формата. Размер: сжатие через zstd для кэша, gzip для передачи по сети -->
