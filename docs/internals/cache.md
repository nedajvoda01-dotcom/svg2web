<!-- [Doc] | Кэширование. Key derivation: SHA256(concat(sha256(svg_content), core_version, sha256(config_json))) — детерминированность, уникальность, версионирование. Sled (disk): embedded KV, ACID transactions, compression, путь ~/.cache/svg2web/, TTL для записей. LRU (memory): lru crate, configurable size limit (1000 entries по умолчанию), thread-safe через Arc<Mutex<_>>. Инвалидация: explicit clean command, автоматическая при изменении версии крейта, TTL для disk entries. Выбор бэкенда: CLI использует disk + memory, WASM только memory, server disk with TTL -->
