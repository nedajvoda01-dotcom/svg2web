<!-- [Doc] | Tera шаблоны. Структура: базовые шаблоны в templates/base/, специфичные для фреймворка в templates/{react,vue,vanilla}/. Контекст: данные из ParseOutput преобразуются в Tera context (colors как CSS переменные, elements как компоненты). Кастомные фильтры: to_camel_case, to_pascal_case, css_value (Color → CSS), px (f64 → "10px"). Наследование: base.html → react/component.tsx, переопределение блоков. Кэширование скомпилированных шаблонов для производительности -->
