# Root Test Snapshots

Этот каталог резервируется под golden files и parser snapshots для development testing.

Минимальный контракт:

- snapshots сопоставляются с файлами из `tests/fixtures/`
- снапшоты используются insta и visual regression сценариями