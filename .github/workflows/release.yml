# [Workflow] | Release pipeline запускается на push tag v* Jobs: verify ci passed build release notes cargo publish --workspace generate changelog build wasm wasm-pack build --target web npm publish crates/svg2web-wasm/pkg create GitHub release с артефактами | ENV: CARGO_REGISTRY_TOKEN secrets.CARGO_REGISTRY_TOKEN NPM_TOKEN secrets.NPM_TOKEN GITHUB_TOKEN secrets.GITHUB_TOKEN | ТРИГГЕР: push tag v* паттерн v[0-9]+.[0-9]+.[0-9]+
