# [Workflow] | CI pipeline запускается на push в main pull_request на любую ветку Jobs: test matrix OS ubuntu-latest windows-latest macos-latest steps checkout install Rust cache cargo build --workspace cargo test --workspace cargo clippy -- -D warnings cargo fmt --check cargo deny check wasm-pack test --headless chrome lint check publish dry-run | ENV: RUST_BACKTRACE=1 CARGO_TERM_COLOR=always | ПРОВЕРКИ: все jobs зеленые fail fast отмена остальных при fail concurrency отмена предыдущих ранов
