# Rust build artifacts
/target/
**/target/

# Cargo lock for libraries (keep for binaries/workspace)
# Cargo.lock — intentionally tracked

# Node.js
node_modules/
web/node_modules/
web/dist/
web/.vite/

# WASM build output (generated by wasm-pack, tracked selectively)
# crates/svg2web-wasm/pkg/ — intentionally tracked (published package)

# OS / IDE
.DS_Store
Thumbs.db
.vscode/settings.json
.idea/

# Codespace / devcontainer
.devcontainer/
*.log
